{
  "method": "GET",
  "headers": {
    {%- for header in source.headers -%}
      {{ header | as_header }},
    {%- endfor -%}
      "Content-Type":"application/json"
  },
  "parameters": {
    "postalCode": "{{ source.body.postalCode }}"
  },
}