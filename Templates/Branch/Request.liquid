{
  "method": "POST",
  "headers": {
    {%- for header in source.headers -%}
      {{ header | as_header }},
    {%- endfor -%}
      "Content-Type":"application/json"
  },
  "body": {
      "PostalCode": "{{ source.body.postalCode }}"
  }
}