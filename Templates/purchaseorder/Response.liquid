{
    "statusCode": {{source.statusCode}},
    "headers": {
        {%- for header in source.headers -%}
        {{ header | as_header }},
        {%- endfor -%}
        "Content-Type": "application/json"
    },
    "body": {
        "orderNumber": "{{ source.body.OrderNumber }}",
        "success": true,
        "errorCode": 0,
        "errorMessage": ""
    }
}
